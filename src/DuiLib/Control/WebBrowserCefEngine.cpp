#include "StdAfx.h"
#include "WebBrowserCefEngine.h"

// stub header-only; this TU keeps the symbol in the static lib when needed
namespace DuiLib
{
	// Intentionally empty — CWebBrowserCefEngine is header-implemented.
}
